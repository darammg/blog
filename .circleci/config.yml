version: 2
jobs:
  build:
    docker:        
      - image: circleci/node:latest
    steps:
      - add_ssh_keys:
        fingerprints:
          - "61:1c:2e:4a:70:e3:e9:23:17:a9:d5:7a:12:01:53:2b"
      - run: 
          name: git pull on ssh
          command: |
            ssh darammg@home.darammg.com -i ~/.ssh/home.darammg.com -o StrictHostKeyChecking=no '
            cd ~/deploy/blog
            git pull'
workflows:
  version: 2
  build:
    jobs:
      - build
