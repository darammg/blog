version: 2
jobs:
  build:
    docker:        
      - image: circleci/alpine
    steps:
      - checkout
      - run: 
          name: git pull on ssh
          command: |
            ssh darammg@home.darammg.com -o StrictHostKeyChecking=no '
            cd ~/deploy/blog
            git pull'
workflows:
  version: 2
  build:
    jobs:
      - build
